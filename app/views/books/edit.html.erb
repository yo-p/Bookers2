<h1>Editing Book</h1>
<% if flash[:notice] %>
  <div class="flash"></div>
<% end %>

<%= form_for(@book) do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>
<% end %>